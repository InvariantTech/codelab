name: Run GitHub App on Pull Request

on:
  pull_request:
    types: [opened, synchronize]

jobs:
  run-github-app:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Run GitHub App
      uses: InvariantTech/invariant-tech@latest
